{
  "name": "ezpz-flexbox-grid-demo",
  "version": "1.0.0",
  "description": "A lightweight Sass (.scss) flexbox grid system that's easy to setup and offers a lot of customisation.",
  "scripts": {
    "postcss": "postcss -u autoprefixer --autoprefixer.browsers \"ie 10, last 2 versions\" -r dist/css/*.css",
    "sass:build": "node-sass src/scss/ -o dist/css/ --output-style uncompressed",
    "sass:watch": "npm run sass:build -- -w",
    "browsersync": "browser-sync start --files \"dist/css/*.css, *.html\" --server",
    "start": "parallelshell \"npm run sass:build\" \"npm run sass:watch\" \"npm run browsersync\"",
    "build": "npm run sass:build & npm run postcss"
  },
  "devDependencies": {
    "autoprefixer": "^6.2.3",
    "browser-sync": "^2.10.1",
    "node-sass": "^3.4.2",
    "parallelshell": "^2.0.0",
    "postcss-cli": "^2.3.3",
    "sass-lint": "^1.4.0"
  },
  "private": true,
  "dependencies": {
    "ezpz-grid": "^2.2.1"
  }
}
